<h1>Listing webpages</h1>

<table>
  <tr>
    <th>Url</th>
  </tr>

<% for webpage in @webpages %>
  <tr>
    <td><%=h webpage.url %></td>
    <td><%= link_to 'Show', webpage_path(webpage) %></td>
    <td><%= link_to 'Edit', edit_webpage_path(webpage) %></td>
    <td><%= link_to 'Destroy', webpage_path(webpage), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New webpage', new_webpage_path %>

