<h1>Listing webpages</h1>

<table>
  <tr>
    <th>Url</th>
  </tr>

<% for webpage in @webpages %>
  <tr>
    <td><%=h webpage.url %></td>
    <td><%= link_to 'Show', webpage %></td>
    <td><%= link_to 'Edit', edit_webpage_path(:id => webpage) %></td>
    <td><%= link_to 'Destroy', webpage, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New webpage', new_webpage_path %>

<br>
<br>
<%= webpages_path %><br>
<%= new_webpage_path %><br>
<%= create_webpage_path %><br>
<%= url_for( { :controller => 'webpages', :action => 'new', :site => 'foo' } ) %><br>
<%= url_for( { :action => 'new', :site => 'foo' } ) %><br>
<%= url_for( { :action => 'new' } ) %><br>
<%= new_webpage_path %><br>
<%= url_for( { :controller => 'webpages', :action => 'edit', :site => 'foo', :id => 3 } ) %>
